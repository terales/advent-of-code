{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cmake-build",
            "type": "shell",
            "options": {
                "cwd": "${fileDirname}"
            },
            "command": "cmake . -D CMAKE_BUILD_TYPE=Debug ; cmake --build ${fileDirname}",
        }
    ]
  }
  